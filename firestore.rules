
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to all documents
    // and restrict writes completely.
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
